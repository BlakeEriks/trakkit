import { NavBarContainer } from '../styles/Nav'

const NavBar = () => {
    return (
        <NavBarContainer>
            <NavTitle title='My App Title'/>
            <NavLink />
        </NavBarContainer>
    )
}

export default NavBar